package com.totallynotacult.jam.map;

import com.badlogic.gdx.graphics.Texture;

public class EnemyTile extends Tile {
    public EnemyTile(Texture texture, int row, int col) {
        super(texture, row, col);
    }
}
